import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const mwo = () => {
  return (
    <View>
      <Text>Migrate Workers and Opportunies</Text>
    </View>
  )
}

export default mwo

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff',
        alignItems: 'center',
        justifyContent: 'center',
      },
})